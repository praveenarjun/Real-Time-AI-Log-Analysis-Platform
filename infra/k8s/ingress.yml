apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: log-analyzer-ingress
  namespace: forensic-platform
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/websocket-services: "go-gateway"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - dashboard.praveen-challa.tech
    - back.praveen-challa.tech
    secretName: tls-secret
  rules:
  - host: back.praveen-challa.tech
    http:
      paths:
      - path: /api/v1/ws/stream
        pathType: Prefix
        backend:
          service:
            name: go-gateway
            port:
              number: 80
      - path: /
        pathType: Prefix
        backend:
          service:
            name: go-gateway
            port:
              number: 80
  - host: dashboard.praveen-challa.tech
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
